<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.xkt.mapper.StoreOrderOperationRecordMapper">

    <insert id="batchInsert" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO store_order_operation_record (target_id, target_type, action, operator_id, operation_time, remark)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.targetId}, #{item.targetType}, #{item.action}, #{item.operatorId}, #{item.operationTime},
            #{item.remark})
        </foreach>
    </insert>
</mapper>