<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BookMapper">
    <resultMap type="Book" id="BookResult">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="author" column="author" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectBookVo">
        SELECT id, name, author, create_time, update_time FROM book
    </sql>

    <select id="selectBookList" resultMap="BookResult">
        <include refid="selectBookVo" />
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="author != null and author != ''">AND author LIKE CONCAT('%', #{author}, '%')</if>
        </where>
    </select>

    <select id="selectBookById" resultMap="BookResult">
        <include refid="selectBookVo" />
        WHERE id = #{id}
    </select>

    <insert id="insertBook" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book (name, author, create_time)
        VALUES (#{name}, #{author}, #{createTime})
    </insert>

    <update id="updateBook">
        UPDATE book
        SET name = #{name}, author = #{author}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteBookById">
        DELETE FROM book WHERE id = #{id}
    </delete>
</mapper>